{
  "name": "coffe-server-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Frontend do Coffee Server — build Vite (MPA) gerando em src/main/resources/static/app",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
